# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Node (if you ever add a build step)
node_modules/
npm-debug.log*

# Local WordPress deploy — never commit the live site copy
/deploy/

# Secrets — never commit real tokens
.env
